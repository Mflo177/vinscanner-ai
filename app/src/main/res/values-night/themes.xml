<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.VinScannerApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Other theme settings -->

        <!-- Popup Menu Dark Theme -->
        <item name="popupMenuStyle">@style/CustomDarkPopupTheme</item>
    </style>

    <style name="CustomDarkPopupTheme" parent="ThemeOverlay.AppCompat.Dark">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@android:color/black</item>
    </style>
</resources>